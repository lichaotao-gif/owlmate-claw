# OwlMate 项目工作约定

默认使用中文交流。不得覆盖已有修改，不得擅自删除重要文件；修改后执行与改动相称的测试，前端修改后使用真实浏览器验证。Git 提交信息使用简洁、准确的中文。

## “开始工作”口令

用户说“开始工作”时：

1. 执行 `npm run work:start`。
2. 确认 Git 同步、依赖检查和本地开发服务均成功。
3. 使用真实浏览器检查 `http://localhost:3018/` 首页能够正常加载。
4. 告知用户预览地址和检查结果，并保持本次开发服务运行。

脚本遇到未提交修改或 Git 冲突时必须停止，说明原因，不得覆盖或丢弃修改。

## “结束工作”口令

用户说“结束工作”时：

1. 检查 Git 状态及实际差异，确认提交范围只包含本次相关修改。
2. 检查不得提交密钥、环境变量、缓存、依赖目录或构建产物。
3. 执行与改动相称的测试；前端修改必须完成真实浏览器验证。
4. 根据实际修改生成简洁、准确的中文提交说明。
5. 执行 `npm run sync:up -- "自动生成的中文提交说明"`。
6. 同步成功后报告修改内容、测试结果、提交编号和远程分支。
7. 确认本次 `work:start` 启动的本地开发服务已经停止。

若没有修改，`sync:up` 不创建空提交，但仍检查远端同步状态。若 pull、rebase 或 push 发生冲突或失败，停止操作并向用户说明，不得擅自覆盖。

## 多电脑顺序

上一台电脑必须先“结束工作”，下一台电脑再“开始工作”。旧电脑首次使用这些命令前执行 `git pull --ff-only origin main`。
